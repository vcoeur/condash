# General
# Shipped by condash. The body below — between this heading and `# Specifics`
# — is refreshed by `condash skills install`. Edits inside this section
# are flagged on the next install. Per-conception patterns go under
# `# Specifics` at the bottom of the file.

# Sentinels: signal that the matching index tree is stale. /projects index
# and /knowledge index clear them. Never commit.
projects/.index-dirty
knowledge/.index-dirty

# Per-item scratch directory next to notes/ — raw inputs, intermediate
# renders, generator scripts. Deliverables live under notes/, not here.
# `**` form catches a `local/` at any depth, not just the canonical
# `projects/YYYY-MM/YYYY-MM-DD-slug/local/` two-level layout.
projects/**/local/
projects/**/local/**

# Local Claude scratch files. Global gitignore convention is **/.claude/*.local.*
.claude/*.local.*

# Per-harness rendered artefacts. condash ships the agent-neutral source — skills
# under `.agents/skills/<name>/` (committed) and the `AGENTS.md` marker region
# (committed) — and the harness launcher renders the per-agent flavours at run
# time. condash itself no longer writes these. They are render output, never
# committed. `.claude/` is file-targeted (not a whole `.claude/*` ignore) because
# it also holds hand-maintained config (settings.json, hooks/). If upgrading from
# a condash that committed these trees, run once:
#     git rm -r --cached .claude/skills .kimi/skills .opencode/skills \
#       .claude/CLAUDE.md .kimi/AGENTS.md .opencode/AGENTS.md
.claude/skills/
.kimi/skills/
.opencode/skills/
.claude/CLAUDE.md
.kimi/AGENTS.md
.opencode/AGENTS.md

# condash workspace state (per-host: settings.json + terminal logs).
# Note: ignoring the dir contents with `.condash/*` (not the dir itself)
# is what lets the negation re-include the starter example. A bare
# `.condash/` would shadow every nested path beyond git's reach.
.condash/*
!.condash/settings.json.example

# Agent API tokens — the per-conception secrets file consumed by the Agents
# pane. The agent definition JSONs under `agents/` ARE committed (they carry
# only a `secretEnv` *name*, never a value); only the secrets file is ignored.
agents/.env

# Common OS / editor cruft.
.DS_Store
Thumbs.db
*.swp
*.swo

# Specifics
# Per-conception patterns below — condash never touches this section on
# `skills install`. Add things like extra `.claude/` overrides, secret
# files, repo-specific scratch dirs, etc.
