# Topics

Cross-cutting subjects that span multiple apps or the ecosystem. Group on disk by whatever subcategories make sense for this workspace — common defaults are `ops/` (deployment, ports, infrastructure), `security/` (auth, privacy, threat models), `testing/` (test harnesses, fixtures, recipes). One file per topic, narrow-scope slug.

## Subdirectories

_No subcategories yet. Create them as needed (e.g. `mkdir topics/ops` and add `topics/ops/index.md`)._
