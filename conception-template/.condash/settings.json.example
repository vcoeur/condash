{
  "$schema_doc": "This tree's condash settings — paths, repositories, tasks. Personal settings (terminal, theme, IDE launchers, dashboard) live in the global settings.json. See docs/reference/config.md.",
  "workspace_path": "/path/to/workspace",
  "worktrees_path": "/path/to/workspace/../worktrees",
  "repositories": []
}
