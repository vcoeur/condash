# Ops

Operational playbooks and gotchas that span multiple apps. Sits under `topics/` rather than `internal/` because the content is product-agnostic — the same recipes apply wherever we run a helio-shaped service.

Start here:

- [Dev ports](dev-ports.md) — the ports each service binds in development, plus the "who's holding the port?" recipe.
